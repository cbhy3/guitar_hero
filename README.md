run:  
python -m http.server  
(must have python)  
notes:  
siberian khatru isn't synced  
to change keys, find keymap array in script.js (near the top) and just change the keys (default: {'a': 0, 's': 1, 'j': 2, 'k':3,'l':4};)
